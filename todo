7. Add js slider. Probably swiper or some alternative
    7.1 Add more js libs.
    7.2 Add basic eslint support
    7.3 Concat all js libs inside ./libs folder
8. Add more meta tags for pug/html files
9. Add handler for sprites (with retina support)
9. Add svgs. Find a way to serve svgs with cross-browser support
10. Add images optimizer

Add htmllint

.
. Implement production version of `default` task (Gulp)
. Implement gulp task for optimized version of dist (like 'dist:optimized')
. Add browserify. Just to make sure that I actually need it
